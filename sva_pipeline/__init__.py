# sva_pipeline: LLM-based SystemVerilog Assertion generation pipeline.
# Uses Qwen3-8B with native tool calling inside a ReAct loop,
# backed by two separate FAISS RAG indices (RTL code and documentation).
